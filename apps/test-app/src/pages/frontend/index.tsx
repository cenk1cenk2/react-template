import { Template } from '@cenk1cenk2/react-template-base'
import React, { Fragment } from 'react'

export const FrontendTemplate: React.FunctionComponent = () => {
  return (
    <Fragment>
      asdadas
      <Template></Template>
    </Fragment>
  )
}

export default FrontendTemplate
